/*
LIMIT: En SQL se utiliza para restringir el número de filas que devuelve una consulta SELECT,
       devolviendo solo un subconjunto de los resultados, lo cual es crucial para la paginación, 
       la optimización del rendimiento y para obtener rápidamente los "Top N" registros (como los 10 productos más vendidos) 
       sin cargar todos los datos. Se coloca al final de la consulta y limita la salida, 
       siendo especialmente útil en bases de datos grandes para mejorar la eficiencia y la usabilidad. 
*/

-- Obtiene las 3 primeras filas de la tabla "users"
SELECT * FROM users LIMIT 3;

-- Obtiene las 2 primeras filas de la tabla "users" con email distinto a sara@gmail.com o edad igual a 15
SELECT * FROM users WHERE NOT email = 'sara@gmail.com' OR age = 15 LIMIT 2;
