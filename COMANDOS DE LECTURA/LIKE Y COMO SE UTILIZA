/*
LIKE: En SQL se utiliza para buscar patrones en cadenas de texto dentro de una columna, 
      no coincidencias exactas, permitiendo filtrar registros usando caracteres comodín como % (cero o más caracteres)
      y _ (un solo carácter) para buscar valores que empiezan, terminan o contienen una secuencia específica de caracteres, 
      siendo ideal para búsquedas flexibles en la cláusula WHERE. 
*/

-- Obtiene todos datos de la tabla "users" que contienen un email con el texto "gmail.com" en su parte final
SELECT * FROM users WHERE email LIKE '%gmail.com';

-- Obtiene todos datos de la tabla "users" que contienen un email con el texto "sara" en su parte inicial
SELECT * FROM users WHERE email LIKE 'sara%';

-- Obtiene todos datos de la tabla "users" que contienen un email una arroba
SELECT * FROM users WHERE email LIKE '%@%';
